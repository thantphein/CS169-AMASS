%h1#listing-title Project Listings

%div#proj-container

  - @projects.each do |project|
    %div.col-sm-4.col-md-3.col-lg-3#project
      %div.thumbnail
        =image_tag "balloon.jpg"
        %div.caption
          %h2= project.category
          %h3= project.organization
          %h4= link_to  project.name, project_path(project.id)
          %p= project.description
          %p= "Budget: #{project.budget}"

%br

