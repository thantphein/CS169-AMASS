%h1#listing-title Project Listings

%div#proj-container

  - @projects.each do |project|
    %div.col-sm-4.col-md-3.col-lg-3#project
      %div.thumbnail=link_to project_path(project.id)
        =image_tag "/#{project.id}/thumb.jpg"
        %div.caption
          %h2= project.category
          %h3{align: "center"}= project.name 
          %h4= project.organization
          %p= project.description
          %p= "Budget: $#{project.budget}"

%br

